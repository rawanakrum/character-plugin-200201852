Implemented motion states:
- WALK-LIKE: locomotion-style controller state using clip ID 12 and 10-joint output.
- PUSH-LIKE: two-hand push-style controller state using clip ID 47 and upper-body/stance joint output.
- CLIMB-LIKE: low-step climb-style controller state using clip ID 83 and asymmetric arm/leg joint output.

Controlled 10 joints:
leftAnkle, rightAnkle,
leftKnee, rightKnee,
leftHip, rightHip,
leftShoulder, rightShoulder,
leftElbow, rightElbow.

Integration summary:
The closed-library character controller is built as character_plugin_200201852.dll. The N8RO visual bridge loads the DLL at runtime, verifies the Arkheon character exports, cycles WALK-LIKE / PUSH-LIKE / CLIMB-LIKE, and registers visual mappings for the available Nathan GLB animation codes. Runtime evidence is shown in evidence/rawan_bridge_latest_excerpt.log with firstJointApply=1 and visualRegistered=1.
